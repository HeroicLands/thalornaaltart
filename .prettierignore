# Build output — regenerated on every run, never hand-edited.
/build/
/nogit/
package-lock.json

# The website. The pages beneath `site/content/` are emitted by the content
# build, and the theme's layouts are Go templates in `.html` files, which
# Prettier reflows as HTML — folding the template directives into prose.
/site/
